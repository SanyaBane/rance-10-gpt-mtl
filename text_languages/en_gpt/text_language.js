/**
 * The translation this repository has shipped from the beginning, and the one
 * a build renders unless .env or --text-lang asks for another.
 *
 * The text itself is the two corpus folders beside this file; README.md says
 * where they came from and what has been done to them since. This manifest is
 * only what the build needs to know about the folder without reading it:
 * whether there is English in it, and one line to print.
 */
export default {
    summary: "the translation this repository has shipped from the beginning",
    translated: true,
};
